import { UpdateContentDirective } from './update-content.directive';

describe('UpdateContentDirective', () => {
  it('should create an instance', () => {
    const directive = new UpdateContentDirective();
    expect(directive).toBeTruthy();
  });
});
